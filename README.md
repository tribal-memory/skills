<!-- PLACEHOLDER (CHECKPOINT 1 scaffold): repo-orientation README authored at CHECKPOINT 6.

Sections (per plan, ~120 lines):
  - What Tribal is (one paragraph + parent repo link).
  - What these skills do (installation + operational; cross-harness).
  - Install one-liner: `npx skills add samfolo/tribal-skills`.
  - Layout map (mirrors the tree at top level).
  - Compatibility matrix (harnesses × scope).
  - LICENCE pointer (CC-BY-4.0).

This README is intentionally short — it orients readers toward `skills/` and the
parent `samfolo/tribal` repo. It is NOT a duplicate of either SKILL.md body.
-->

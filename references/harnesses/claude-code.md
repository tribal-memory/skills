<!-- PLACEHOLDER (CHECKPOINT 1 scaffold): authored at CHECKPOINT 5.

Claude Code wire-up:
  - Primary doc URL + `fetched: <date>` (verify against
    https://docs.claude.com/en/docs/claude-code/mcp at the time of authoring).
  - Native config shape (accepts `tribal mcp-config` JSON directly via
    `claude mcp add-json`).
  - jq translation from canonical `tribal mcp-config --json` output → the
    `claude mcp add-json` invocation.
  - Wire-up one-liner.
  - Ask-first consent reminder → references/consent.md.

Target: ~60 lines.
-->

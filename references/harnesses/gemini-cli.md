<!-- PLACEHOLDER (CHECKPOINT 1 scaffold): authored at CHECKPOINT 5.

Gemini CLI wire-up.

  - Primary doc URL + `fetched: <date>` (verify against the canonical
    Gemini CLI docs at the time of authoring).

  - Native config shape (`httpUrl` for Streamable HTTP).

  - jq translation from canonical `tribal mcp-config --json` output →
    the Gemini CLI shape.

  - Wire-up one-liner.

  - Ask-first consent reminder → references/consent.md.

Target: ~60 lines.

(Internal author note, not user-visible: Gemini CLI has a published
deprecation date in favour of Antigravity. Maintain both; do not editorialise
on the vendor lifecycle in user-visible content. Author effort can be biased
toward the tool with a larger installed base but the published wire-up gets
equal treatment.)
-->

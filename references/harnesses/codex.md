<!-- PLACEHOLDER (CHECKPOINT 1 scaffold): authored at CHECKPOINT 5.

Codex (OpenAI) wire-up:
  - Primary doc URL + `fetched: <date>` (verify against
    https://developers.openai.com/codex/mcp at the time of authoring).
  - Native config shape (TOML with `bearer_token_env_var`).
  - jq translation from canonical `tribal mcp-config --json` output → the TOML shape.
  - Wire-up one-liner.
  - Ask-first consent reminder → references/consent.md.

Target: ~60 lines.
-->
